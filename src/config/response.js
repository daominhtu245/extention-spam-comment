const statusFail = 0;
const statusSuccess = 1;
const statusNotVip = 2;
const statusNoLogin = 3;

export const status = {
    fail: statusFail,
    success: statusSuccess,
    not_vip: statusNotVip,
    no_login: statusNoLogin,
}
